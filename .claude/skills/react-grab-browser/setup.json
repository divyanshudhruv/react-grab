{
  "setupComplete": false,
  "prerequisites": {
    "chrome": {
      "required": true,
      "installed": false,
      "description": "Google Chrome browser"
    },
    "cli": {
      "required": true,
      "installed": false,
      "description": "@react-grab/cli available (via npx or global install)"
    }
  },
  "setupInstructions": [
    "1. Ensure Google Chrome is installed on your system",
    "2. Test the CLI: npx -y @react-grab/cli browser execute \"return 'hello'\"",
    "3. If test succeeds, update this setup.json: set 'setupComplete' to true"
  ],
  "verifySetup": "Run 'npx -y @react-grab/cli browser execute \"return await snapshot()\"' to verify installation"
}
